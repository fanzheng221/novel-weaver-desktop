{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local desktop application.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
